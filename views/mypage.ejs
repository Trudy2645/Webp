<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Pretendard&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Pretendard', sans-serif;
            background-color: #f4f6f9;
            margin: 0;
            padding: 0;
        }
        header {
            background-color: #ffffff;
            border-bottom: 1px solid #dee2e6;
            padding: 1.2rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        header h1 {
            font-weight: 800;
            font-size: 1.8rem;
            margin: 0;
            color: #212529;
        }
        nav a {
            margin-left: 1.5rem;
            color: #495057;
            text-decoration: none;
            font-weight: 500;
        }
        nav a:hover {
            color: #0d6efd;
        }
        main {
            padding: 3rem 5%;
            text-align: center;
        }
        .mypage-container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0,0,0,0.06);
            width: 600px;
            margin: 40px auto;
            text-align: left;
        }
        .mypage-container h2 {
            text-align: center;
            font-size: 2rem;
            color: #343a40;
            margin-bottom: 30px;
        }
        .user-info-grid {
            display: grid;
            grid-template-columns: 150px 1fr; /* 라벨과 값의 너비 */
            gap: 15px 20px;
            margin-bottom: 30px;
        }
        .user-info-grid strong {
            color: #495057;
            font-weight: 600;
        }
        .user-info-grid span {
            color: #212529;
        }
        .action-buttons {
            text-align: center;
        }
        .action-buttons .btn {
            margin: 0 10px;
        }
        footer {
            margin-top: 4rem;
            padding: 2rem 0;
            background-color: #ffffff;
            border-top: 1px solid #dee2e6;
            color: #868e96;
            font-size: 0.9rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        <h1>🪬 잡(Job)귀야 물럿거라</h1>
        <nav>
            <a href="/">홈</a>
            <a href="/products">상품목록</a>
            <a href="/cart">장바구니</a>
            <a href="/wishlist">위시리스트</a>
            <a href="/board">고객센터</a>
            <% if (user) { %>
                <a href="/user/mypage">마이페이지</a>
                <a href="/user/logout">로그아웃</a>
            <% } else { %>
                <a href="/login">로그인</a>
                <a href="/user/register">회원가입</a>
            <% } %>
        </nav>
    </header>

    <main>
        <div class="mypage-container">
            <h2>환영합니다, <%= userDetails.name %>님!</h2>
            <div class="user-info-grid">
                <strong>아이디:</strong> <span><%= userDetails.username %></span>
                <strong>이름:</strong> <span><%= userDetails.name %></span>
                <strong>생년월일:</strong> <span><%= userDetails.birthdate || '미입력' %></span>
                <strong>성별:</strong> <span><%= userDetails.gender || '미입력' %></span>
                <strong>주소:</strong> <span><%= userDetails.address || '미입력' %></span>
                <strong>전화번호:</strong> <span><%= userDetails.phone_number || '미입력' %></span>
                <strong>이메일:</strong> <span><%= userDetails.email || '미입력' %></span>
                <strong>SMS 수신 동의:</strong> <span><%= userDetails.sms_consent ? '예' : '아니오' %></span>
                <strong>이메일 수신 동의:</strong> <span><%= userDetails.email_consent ? '예' : '아니오' %></span>
                <strong>개인정보 처리 동의:</strong> <span><%= userDetails.privacy_consent ? '예' : '아니오' %></span>
            </div>
            <div class="action-buttons">
                <a href="/user/edit-profile" class="btn btn-primary">내 정보 수정</a>
                <a href="/user/change-password" class="btn btn-secondary">비밀번호 변경</a>
                </div>
        </div>
    </main>

    <footer>
        <p>&copy; 2025 쇼핑몰 예시. All rights reserved.</p>
    </footer>
</body>
</html>