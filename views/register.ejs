<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원가입</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Jua&display=swap');

        body {
            margin: 0;
            padding: 0;
            font-family: 'Jua', sans-serif;
            background-color: #0d2b45;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh; /* height 대신 min-height 사용 */
            padding: 20px 0; /* 내용이 길어질 경우를 대비해 패딩 추가 */
            box-sizing: border-box; /* 패딩이 전체 크기에 포함되도록 */
        }

        .register-container {
            background-color: #ffe480;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 6px 16px rgba(0,0,0,0.3);
            width: 400px; /* 너비 약간 증가 */
            text-align: center;
            color: #0d2b45;
            overflow-y: auto; /* 내용이 길어질 경우 스크롤 허용 */
            max-height: 90vh; /* 최대 높이 설정 */
        }

        .register-container h2 {
            font-size: 28px;
            margin-bottom: 20px;
        }

        .form-group { /* 새로운 폼 그룹 스타일 */
            margin-bottom: 15px;
            text-align: left; /* 라벨 정렬 */
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-size: 15px;
            font-weight: bold;
        }

        .register-container input[type="text"],
        .register-container input[type="password"],
        .register-container input[type="email"],
        .register-container input[type="date"], /* date 타입 추가 */
        .register-container select { /* select 박스 추가 */
            width: calc(100% - 28px); /* 패딩 고려 */
            padding: 14px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            box-sizing: border-box; /* 패딩이 너비에 포함되도록 */
            margin-top: 5px; /* 라벨과의 간격 */
        }

        .register-container .radio-group { /* 성별 라디오 버튼 그룹 */
            display: flex;
            gap: 20px;
            margin-top: 10px;
            justify-content: center; /* 가운데 정렬 */
            font-size: 15px;
            font-weight: normal; /* 라벨과 동일하게 굵기 조절 */
        }
        .register-container .radio-group input[type="radio"] {
            width: auto; /* 라디오 버튼 너비 조정 */
            margin: 0 5px 0 0;
            padding: 0;
        }
        .register-container .radio-group label {
            margin-bottom: 0;
        }

        .register-container .checkbox-group { /* 동의 체크박스 그룹 */
            text-align: left;
            margin-top: 20px;
            border-top: 1px solid #ccc;
            padding-top: 15px;
        }
        .register-container .checkbox-group div {
            margin-bottom: 10px;
            display: flex;
            align-items: flex-start;
        }
        .register-container .checkbox-group input[type="checkbox"] {
            width: auto; /* 체크박스 너비 조정 */
            margin: 0 8px 0 0; /* 오른쪽 여백 추가 */
            align-self: center; /* 세로 중앙 정렬 */
        }
        .register-container .checkbox-group label {
            font-size: 14px;
            font-weight: normal;
            display: inline; /* 라벨 텍스트가 체크박스 옆에 오도록 */
            margin-bottom: 0;
        }

        .register-container button {
            width: 100%;
            padding: 14px;
            background-color: #ff6b00;
            color: white;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-top: 25px; /* 버튼 상단 여백 추가 */
        }

        .register-container button:hover {
            background-color: #e05400;
        }

        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: #0d2b45;
            font-size: 14px;
            text-decoration: underline;
        }

        .emoji {
            font-size: 40px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="emoji">📝</div>
        <h2>회원가입</h2>
        <form action="/user/register" method="post">
            <div class="form-group">
                <label for="username">아이디</label>
                <input type="text" id="username" name="username" placeholder="아이디" required>
            </div>
            
            <div class="form-group">
                <label for="name">이름</label>
                <input type="text" id="name" name="name" placeholder="이름" required>
            </div>
            
            <div class="form-group">
                <label for="password">비밀번호</label>
                <input type="password" id="password" name="password" placeholder="비밀번호" required>
            </div>

            <div class="form-group">
                <label for="birthdate">생년월일</label>
                <input type="date" id="birthdate" name="birthdate">
            </div>

            <div class="form-group">
                <label>성별</label>
                <div class="radio-group">
                    <input type="radio" id="gender_male" name="gender" value="남">
                    <label for="gender_male">남</label>
                    <input type="radio" id="gender_female" name="gender" value="여">
                    <label for="gender_female">여</label>
                    <input type="radio" id="gender_none" name="gender" value="선택 안 함" checked>
                    <label for="gender_none">선택 안 함</label>
                </div>
            </div>

            <div class="form-group">
                <label for="address">주소</label>
                <input type="text" id="address" name="address" placeholder="주소">
            </div>
            
            <div class="form-group">
                <label for="email">이메일</label>
                <input type="email" id="email" name="email" placeholder="이메일">
            </div>
            
            <div class="form-group">
                <label for="phone">휴대폰 번호</label>
                <input type="text" id="phone" name="phone" placeholder="예: 010-1234-5678">
            </div>

            <div class="checkbox-group">
                <p style="font-weight: bold; margin-bottom: 10px;">동의 및 약관</p>
                <div>
                    <input type="checkbox" id="email_consent" name="email_consent">
                    <label for="email_consent">이메일 수신 동의</label>
                </div>
                <div>
                    <input type="checkbox" id="personal_info_consent" name="personal_info_consent" required>
                    <label for="personal_info_consent">개인정보 수집 및 이용 동의 (필수)</label>
                </div>
                <div>
                    <input type="checkbox" id="sms_consent" name="sms_consent">
                    <label for="sms_consent">SMS 수신 동의</label>
                </div>
            </div>

            <button type="submit">가입하기</button>
        </form>
        <a class="back-link" href="/">🏠 메인으로 돌아가기</a>
    </div>
</body>
</html>